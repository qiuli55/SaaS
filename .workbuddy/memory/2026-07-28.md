## 2026-07-28

### 00:18 - 排查 "AI无法正常使用" 问题

**验证结果：后端和服务端全部正常**
- DeepSeek API key 有效，直接调用返回正常
- 后端 `/api/documents/generate` 通过 curl 测试成功生成完整法律文书（律师函、民事起诉状）
- 前端 Vite 代理 `/api/* → http://127.0.0.1:8000` 工作正常
- 完整端到端链路过：登录 → 获取案件 → AI生成文书 均 200 OK

**发现：前端启动脚本兼容性**
- `node node_modules/.bin/vite` 失败（.bin/vite 是 bash 脚本不能直接用 node 执行）
- 正确方式：`node node_modules/vite/bin/vite.js --host 0.0.0.0 --port 5173`
- 前后端均已重启（PID 44220 / 新Vite进程）

**待查：用户端浏览器**
- 建议 Ctrl+F5 硬刷新，打开 F12 控制台查看具体错误
