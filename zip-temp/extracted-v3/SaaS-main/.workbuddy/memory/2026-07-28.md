## 2026-07-28

### 新增功能（V1 完善 + V2 起步）

**案件编辑与删除：**
- 新增加 CaseEditView.vue（编辑页，复用创建页面布局，支持全部字段修改 + 删除按钮）
- CaseDetailView 新增加"编辑"按钮
- 路由 `/cases/:id/edit`

**PDF 导出：**
- 使用 fpdf2 实现文书 PDF 导出
- 自动检测 Windows 中文字体（SimSun/SimFang/微软雅黑）
- 按法律文书格式渲染（标题居中加粗、此致右对齐、落款右对齐）
- 无中文字体时兜底用纯文本
- 前端 DocumentViewView + DocumentNewView 均新增"下载 PDF"按钮

**客户管理模块（V2 CRM）：**
- 后端：clients 表 + 完整 CRUD API（列表/创建/详情/编辑/删除）
- 前端：ClientListView（搜索+分页）、ClientNewView（标签选择+自定义标签）、ClientDetailView（信息+关联案件列表）
- Case 模型新增 client_id 外键，支持案件关联客户
- 顶部导航栏新增"客户管理"入口
- Case 的 API 输出增加 client_id/client_name 字段

### Git
- 推送到 qiuli55/SaaS，commit: e20df24

### 01:11 - 修复 AI 无法使用：Vue 模板 JS 表达式里误用 HTML 实体

**问题根因**：用户截图报 `[plugin:vite:vue] Error parsing JavaScript expression: Unterminated string constant. (1:1)`
- 错误位置：`frontend/src/views/DocumentNewView.vue:115:27`
- 真实原因：第115行的 `:placeholder="'...&#10;...'"` 里把 HTML 实体 `&#10;` 误用到了 Vue 模板的 JS 表达式中。在 JS 字符串字面量里 `&#10;` 就是 6 个普通字符 `& # 1 0 ;`，导致解析器误判字符串未闭合
- 修复：改用 ES6 模板字符串 + `\n`（真换行符）+ `${}` 插值
  - 旧：`'详细...&#10;2026年1月15日，' + caseInfo.plaintiff + '与' + caseInfo.defendant + ...`
  - 新：`` `详细...\n2026年1月15日，${caseInfo.plaintiff}与${caseInfo.defendant}...` ``
- Vite HMR 自动重编译，curl `/src/views/DocumentNewView.vue` 已无 error

**关键教训**：Vue 模板 `:attr="..."` 表达式是**纯 JS**，HTML 实体（`&#10;`、`&amp;` 等）**不会解码**。需要换行用 `\n`，需要插值用 `${}`（模板字符串）或字符串拼接

### 01:12 - 批量生成、版本回溯、新建页完善

**批量生成文书：**
- 后端 POST /api/documents/generate-batch 批量接口
- 前端 BatchGenerateView：选类型 → 公共信息 → 多行当事人 → 一键生成
- 支持快速粘贴（空格/Tab 分隔的原告 被告 金额）
- 结果列表显示成功/失败，可直接下载/查看

**文书版本回溯：**
- API GET /api/documents/{id}/versions 获取同类型所有版本
- DocumentViewView 顶部版本切换器，切换查看不同版本内容

**新建案件页完善：**
- CaseNewView 新增当事人详细信息折叠表单（原告/被告详情 + 管辖法院）
- 创建时保存 plaintiff_detail/defendant_detail JSON，生成文书时自动带入

### 02:22 - 全面筛查补全（安全加固 + 功能补全 + 代码质量修复）

**Critical 修复（安全+阻断）：**
- **文件访问安全漏洞修复**：preview/download/delete 三个端点均添加用户所有权校验 `_get_user_file()`，防止跨用户文件访问
- **日程 Pydantic 验证**：schedules.py 从裸 `dict` 改为 `ScheduleCreate`/`ScheduleUpdate` schema，增加 event_type 枚举校验和 case_id 合法性验证
- **case_no 竞态条件修复**：从 `count()+1` 改为 `时间戳+uuid4 hex` 方案
- **python-dotenv 支持**：main.py 优先使用 dotenv 加载 .env，兜底手动解析
- **健康检查增强**：增加数据库连接检测
- **SECRET_KEY 强化**：auth.py 移除弱默认值，未设置时直接抛 RuntimeError
- **JWT decode 加固**：增加 `verify_exp` 和 `require: ["exp","sub"]` 选项
- **requirements.txt 更新**：passlib→bcrypt4.2.1，新增 fpdf2、python-dotenv
- **.gitignore 创建**：排除 .env、*.db、uploads/

**High 修复（功能缺失）：**
- **文书编辑/删除**：新增 PUT/DELETE /api/documents/{doc_id}
- **用户更新**：新增 PUT /api/user/profile、PUT /api/user/password
- **用户密码修改**：原密码验证 + 新旧密码不同校验

**Frontend Critical 修复：**
- **404 路由**：添加 `/:pathMatch(.*)*` 兜底重定向到首页
- **弹窗 CSS**：在主样式表新增完整 modal 样式（overlay/modal/header/body/footer + 入场动画）
- **App.vue 标题映射**：补充 Calendar→日历日程
- **12 处空 catch{} 修复**：CaseDetailView(3)、CaseEditView(1)、ClientDetailView(2)、CalendarView(3)、DocumentNewView(3)、DocumentViewView(2)、HistoryView(1) 全部加了错误处理

**Frontend High 修复：**
- **ClientListView 分页**：补充分页参数和分页 UI 控件
- **DashboardView 占位文本**："V3上线"→"未完成日程"
- **文件下载认证**：CaseDetailView 预览/下载改用 token 认证方式

**Medium 修复：**
- **models.py 完善**：Document/CaseFile/Client/Schedule 均添加 updated_at 字段；Schedule 增加 User/Case relationship；Schedule.case_id 改为 nullable
- **schemas.py 扩充**：新增 ScheduleCreate/ScheduleUpdate/ScheduleInfo/UserUpdate/PasswordChange
- **documents.py 嵌套 except**：`except:`→`except JSONDecodeError:`

**验证结果：**
- 后端重启成功，健康检查 `{"status":"ok","database":"connected"}`
- 登录/注册/日程/案件 API 全部测试通过
- 测试账号 13800138000 / 123456 正常可用
